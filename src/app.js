oSelectOne = oForm.elements["select_one_element_name"];
index = oSelectOne.selectedIndex; 


var selected_option_text = oSelectOne.options[index].text; 

var selected_index = oForm.elements["which-animal"].selectedIndex;

if(selected_index > 0)
{
   
   var selected_option_text = oForm.elements["which-animal"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');



   
}



var selected_index = oForm.elements["best-trait"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["best-trait"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}




var selected_index = oForm.elements["worst-trait"].selectedIndex;

if(selected_index > 0)
{

   var selected_option_text = oForm.elements["worst-trait"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}



var selected_index = oForm.elements["which-wizard"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["which-wizard"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}




var selected_index = oForm.elements["least-like"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["least-like"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}




var selected_index = oForm.elements["remember-me"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["remember-me"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}




var selected_index = oForm.elements["which-potion"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["which-potion"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}




var selected_index = oForm.elements["which-box"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["which-box"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}




var selected_index = oForm.elements["river-troll"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["river-troll"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}




var selected_index = oForm.elements["enchanted-garden"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["enchanted-garden"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}


var selected_index = oForm.elements["which-goblet"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["which-goblet"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}


var selected_index = oForm.elements["what-power"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["what-power"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}



var selected_index = oForm.elements["which-road"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["which-road"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}



var selected_index = oForm.elements["which-subject"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["which-subject"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}



var selected_index = oForm.elements["rather-be"].selectedIndex;

if(selected_index > 0)
{
  
   var selected_option_text = oForm.elements["rather-be"].options[selected_index].text;
}
else
{
   alert('Please select a country from the drop down list');
}







